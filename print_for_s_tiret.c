#include "Struct_d_and_i.h"
#include "print.h"

int		ft_print_for_s_tiret(int etoile,int etoile2, const char *s, int i, char *string)
{
	int c;
	int count;
	char	*tmp = NULL;
	char	*dest = NULL;

	ft_flag_tiret_s(i + 1, s, string);
	return (i);
}